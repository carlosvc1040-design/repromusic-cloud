{
  "name": "repromusic-cloud",
  "version": "2.0.0",
  "description": "ReproMusic Cloud Server - YouTube audio streaming API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "chmod +x build.sh && ./build.sh"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
